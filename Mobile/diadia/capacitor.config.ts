import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dia.gustavo',
  appName: 'diaadia',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
